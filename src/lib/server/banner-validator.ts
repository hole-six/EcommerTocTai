import { z } from "zod";
export const bannerSchema = z.object({ pageKey: z.string().default("home"), slotKey: z.string().default(""), placement: z.enum(["home_hero", "home_promo", "all_products", "category"]).default("home_hero"), categorySlug: z.string().default(""), mediaType: z.enum(["image", "video"]).default("image"), image: z.string().min(1), mobileImage: z.string().default(""), videoUrl: z.string().default(""), alt: z.string().min(2).max(160), title: z.string().max(160).default(""), subtitle: z.string().max(400).default(""), ctaLabel: z.string().max(80).default("Khám phá ngay"), ctaHref: z.string().max(500).default("/shop/all"), isActive: z.boolean().default(true), sortOrder: z.number().int().min(0).default(0) });
