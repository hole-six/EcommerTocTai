import { z } from "zod";
export const bannerSchema = z.object({ pageKey: z.string().default("home"), slotKey: z.string().default(""), placement: z.enum(["home_hero", "home_promo", "all_products", "category", "site_bar", "home_men_videos", "home_concerns", "site_contact_buttons"]).default("home_hero"), categorySlug: z.string().default(""), mediaType: z.enum(["image", "video"]).default("image"), image: z.string().default(""), mobileImage: z.string().default(""), videoUrl: z.string().default(""), alt: z.string().max(160).default("Banner Tóc Tai"), title: z.string().max(160).default(""), subtitle: z.string().max(400).default(""), ctaLabel: z.string().max(80).default("Khám phá ngay"), ctaHref: z.string().max(500).default("/shop/all"), isActive: z.boolean().default(true), sortOrder: z.number().int().min(0).default(0) });
